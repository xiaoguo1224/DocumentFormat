[project]
name = "document-format-writer"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = ["python-docx>=1.1", "lxml>=5", "PyYAML>=6"]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
